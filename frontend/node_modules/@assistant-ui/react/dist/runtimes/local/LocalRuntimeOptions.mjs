// src/runtimes/local/LocalRuntimeOptions.tsx
var splitLocalRuntimeOptions = (options) => {
  const { initialMessages, maxToolRoundtrips, maxSteps, adapters, ...rest } = options;
  return {
    localRuntimeOptions: {
      initialMessages,
      maxToolRoundtrips,
      maxSteps,
      adapters
    },
    otherOptions: rest
  };
};
export {
  splitLocalRuntimeOptions
};
//# sourceMappingURL=LocalRuntimeOptions.mjs.map